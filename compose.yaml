services:
  app:
    image: nextbike-collector:latest
    container_name: nextbike-collector
    restart: unless-stopped
    volumes:
      - ./values.yaml:/app/values.yaml
      - ./output-json:/app/output-json
      - ./output-parquet:/app/output-parquet